global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alert_rules.yml

scrape_configs:
  - job_name: "platform-api"
    static_configs:
      - targets: ["app:8000"]
    metrics_path: /metrics
